@model IEnumerable<BibliofilAPI.Models.Loan>

@{
    ViewData["Title"] = "Index";
}

<h1>Loans</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="min-w-full divide-y divide-gray-200 border border-gray-300">
    <thead>
        <tr>
            <th class="px-4 py-2 text-left font-semibold text-gray-700 border-b">
                @Html.DisplayNameFor(model => model.User.Id)
            </th>
            <th class="px-4 py-2 text-left font-semibold text-gray-700 border-b">
                @Html.DisplayNameFor(model => model.Book.Id)
            </th>
            <th class="px-4 py-2 text-left font-semibold text-gray-700 border-b">
                @Html.DisplayNameFor(model => model.Book.Title)
            </th>
            <th class="px-4 py-2 text-left font-semibold text-gray-700 border-b">
                @Html.DisplayNameFor(model => model.LoanDate)
            </th>
            <th class="px-4 py-2 text-left font-semibold text-gray-700 border-b"></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td class="px-4 py-2 border-b">
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td class="px-4 py-2 border-b">
                @Html.DisplayFor(modelItem => item.Book.Id)
            </td>
            <td class="px-4 py-2 border-b">
                @Html.DisplayFor(modelItem => item.Book.Title)
            </td>
            <td class="px-4 py-2 border-b">
                @Html.DisplayFor(modelItem => item.LoanDate)
            </td>
            <td class="px-4 py-2 border-b">
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
