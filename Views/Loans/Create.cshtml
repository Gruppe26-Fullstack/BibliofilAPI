@model BibliofilAPI.Models.Loan

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Loan</h4>
<hr />
<div class="flex justify-center">
    <div class="w-full max-w-md">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-sm text-red-500"></div>
            <span asp-validation-for="BookId" class="text-sm text-red-500"></span>
            <span asp-validation-for="UserId" class="text-sm text-red-500"></span>
            <div class="mb-4">
                <label asp-for="LoanDate" class="block text-sm font-medium text-gray-700 mb-1"></label>
                <input asp-for="LoanDate" class="w-full rounded-lg border border-gray-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" />
                <span asp-validation-for="LoanDate" class="text-sm text-red-500"></span>
            </div>
            <div class="mb-4">
                <label asp-for="UserId" class="block text-sm font-medium text-gray-700 mb-1"></label>
                <select asp-for="UserId" class ="w-full rounded-lg border border-gray-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="mb-4">
                <label asp-for="BookId" class="block text-sm font-medium text-gray-700 mb-1">Book Title</label>
                <select asp-for="BookId" class ="w-full rounded-lg border border-gray-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" asp-items="ViewBag.BookId"></select>
            </div>
            <div class="mb-4">
                <input type="submit" value="Create" class="rounded-lg bg-blue-600 px-4 py-2 text-white hover:bg-blue-700" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
